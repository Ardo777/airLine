spring:
  datasource:
    url: jdbc:mysql://localhost:3306/air_line
    username: root
    password: root
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 10MB
  mail:
    host: smtp.gmail.com
    port: 587
    username: javamailsender2024@gmail.com
    password: uiiz kuxy pcfo zmfp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  jpa:
    hibernate:
      ddl-auto: none
  liquibase:
    change-log: classpath*:db/change-log.xml
  devtools:
    restart:
      log-condition-evaluation-delta: false
picture:
  upload:
    directory: ./pictures